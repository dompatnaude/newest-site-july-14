# newest-site-july-14
chmod +x deploy.sh
./deploy.sh deploy-site
# or, if creating a new remote:
git remote add origin git@github.com:YOUR_USER/polar-pep-look-alike.git
./deploy.sh deploy-site origin
